# List Entities

- An authenticated user can view the entities that exist in his account.
- An authenticated user can select an entity as the current working entity.
- An authenticated user can see which entity is currently selected.
